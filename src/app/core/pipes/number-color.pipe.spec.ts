import { NumberColorPipe } from './number-color.pipe';

describe('NumberColorPipe', () => {
  it('create an instance', () => {
    const pipe = new NumberColorPipe();
    expect(pipe).toBeTruthy();
  });
});
